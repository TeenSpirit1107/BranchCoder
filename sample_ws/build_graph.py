"""
Module for building directed graphs
Creates a directed graph with 6 nodes and specifies a start node
"""


def build_graph():
    """
    Build a directed graph with 6 nodes
    Returns: (graph, start_node)
    graph: dictionary, keys are nodes, values are lists containing tuples of (target_node, weight)
    start_node: starting node
    """
    # TODO: Implement the build_graph function
    # Create a directed graph with 6 nodes (0-5) using adjacency list representation
    # Graph structure should be:
    #   Node 0: edges to nodes 1 (weight 4) and 2 (weight 2)
    #   Node 1: edges to nodes 2 (weight 1) and 3 (weight 5)
    #   Node 2: edges to nodes 3 (weight 8) and 4 (weight 10)
    #   Node 3: edges to nodes 4 (weight 2) and 5 (weight 6)
    #   Node 4: edge to node 5 (weight 3)
    #   Node 5: no outgoing edges (empty list)
    # Set start_node to 0
    # Return (graph, start_node)
    pass

